# Request definition
[request_definition]
r = sub, obj, act

# Policy defintion
[policy_definition]
p = sub, obj, act

# Policy effect
[policy_effect]
e = some(where (p.eft == alow))

# Matchers
[matchers]
m = r.sub === p.sub && r.obj == p.obj && r.act == p.act
